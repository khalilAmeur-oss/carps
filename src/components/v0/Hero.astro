---
// Converted from Hero.jsx
---
) => clearTimeout(timer);
  }, []);

  return (
    <section class={`hero ${revealed ? 'revealed' : ''}`} id="hero" aria-labelledby="hero-heading">
      <div class="hero__bg" aria-hidden="true" style={{ position: 'absolute', inset: 0, overflow: 'hidden' }}>
        <video 
          src={heroVideo}
          autoPlay 
          muted 
          loop 
          playsInline 
          preload="metadata"
          class="hero__bg-video" 
          style={{ width: '100%', height: '100%', objectFit: 'cover' }}
        ></video>
        {/* Overlay to ensure text readability against video */}
        <div style={{ position: 'absolute', inset: 0, backgroundColor: 'var(--primary-navy)', opacity: 0.8, mixBlendMode: 'multiply' }}></div>
        <div style={{ position: 'absolute', inset: 0, backgroundColor: 'rgba(7, 26, 43, 0.4)' }}></div>
      </div>
      
      <div class="hero__container">
        <div class="hero__content">
          <span class="hero__eyebrow">[CONTENT]</span>
          <h1 class="hero__title" id="hero-heading">[CONTENT]</h1>
          <p class="hero__subtitle">[CONTENT]</p>
          <p class="hero__description">[CONTENT]</p>
          
          <div class="hero__stats">
            <div class="hero__stat">
              <span class="hero__stat-number">[CONTENT]</span>
              <span class="hero__stat-label">[CONTENT]</span>
            </div>
            <div class="hero__stat">
              <span class="hero__stat-number">[CONTENT]</span>
              <span class="hero__stat-label">[CONTENT]</span>
            </div>
            <div class="hero__stat">
              <span class="hero__stat-number">[CONTENT]</span>
              <span class="hero__stat-label">[CONTENT]</span>
            </div>
          </div>
          
          <div class="hero__actions">
            <a href="#contact" class="btn btn--gold btn--lg">[CONTENT] <span aria-hidden="true">→</span></a>
            <div class="hero__secondary">
              <span class="hero__secondary-text">[CONTENT]</span>
              <a href="tel:+33148245520" class="hero__phone-link">+33 1 48 24 55 20</a>
            </div>
          </div>
        </div>
        
        <div class="hero__form-wrapper">
          <QuoteForm formId="hero" variant="hero" />
          
          <div class="hero__reassurance">
            <span class="hero__reassurance-item">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#b08b57" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" style={{marginRight: '8px'}}>
                <polyline points="20 6 9 17 4 12"></polyline>
              </svg>
              [CONTENT]
            </span>
            <span class="hero__reassurance-item">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#b08b57" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" style={{marginRight: '8px'}}>
                <polyline points="20 6 9 17 4 12"></polyline>
              </svg>
              [CONTENT]
            </span>
          </div>
        </div>
      </div>
    </section>
  
